include 'midifi/__init__';

automode := Env::getenv 'AUTOMODE' == 'true';
values := make_values$ if automode
    then #desktop_mode
    else VideoMode (800u,  600u) endif;
w := RootWindow automode;
#(w.run);
